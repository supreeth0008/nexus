---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-broken-app
  namespace: nexus-demo
  annotations:
    nexus.incident/id: "ed44bf42-1619-4f3c-b898-8746f37e9327"
    nexus.incident/type: "reliability_degradation"
    nexus.fix/reason: "Reliability drop – error rate / crashloop"
spec:
  template:
    spec:
      containers:
      - name: app
        image: nginx:latest
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
# I generated this Kubernetes fix for incident ed44bf42
# Original image: nginx:this-tag-does-not-exist-12345
